import {
  ActionResponse,
  CreateOrderPayload,
  GetListResponse,
  GetOneResponse,
} from "../types";
import { decodeBearerToken } from "../utils";
import express from "express";
import { CreateProductRatePayload } from "../types/ProductRate";

//create order (check out functions)
const createProductRate = async (req: CreateProductRatePayload, res: ActionResponse) => {
 try {
  
 } catch (error) {
  
 }
};



export {createProductRate };
